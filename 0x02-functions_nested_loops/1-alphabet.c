#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void) {
    char curr;
    for (curr = 'a'; curr <= 'z'; curr++) {
        _putchar(curr);
    }
    _putchar('\n');
}
